# Optional — leave empty to use Vite dev proxy (/api → http://127.0.0.1:8000)
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Production (Vercel) — Railway public HTTPS origin, no trailing slash
# VITE_API_BASE_URL=https://<railway-service>.up.railway.app
